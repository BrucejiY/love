# 快速启动指南 🚀

10 分钟内运行朋友圈恋爱助手！

## 🎯 前置要求

- ✅ Node.js 18+ 
- ✅ npm 或 yarn
- ✅ 豆包大模型 API Key（[免费获取](https://console.volcengine.com/ark)）

## ⚡ 5 步快速启动

### 第 1 步：安装依赖

```bash
# 进入项目目录
cd /Users/bruce/Desktop/demo

# 安装前端依赖
cd frontend
npm install

# 安装后端依赖
cd ../backend
npm install
```

### 第 2 步：配置 API Key

```bash
# 在 backend 目录创建 .env 文件
cd backend
nano .env
```

输入以下内容（替换为你的真实 API Key）：

```env
ARK_API_KEY=你的豆包API密钥
ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
PORT=3001
NODE_ENV=development
UPLOAD_DIR=./uploads
```

保存文件（Ctrl+O, Enter, Ctrl+X）

### 第 3 步：启动后端

```bash
# 在 backend 目录
npm run dev
```

看到以下信息说明成功：
```
🚀 服务器运行在 http://localhost:3001
🤖 豆包 API: 已配置
```

### 第 4 步：启动前端

**打开新终端窗口**，执行：

```bash
cd /Users/bruce/Desktop/demo/frontend
npm run dev
```

看到以下信息说明成功：
```
VITE ready in xxx ms
➜  Local:   http://localhost:5173/
```

### 第 5 步：访问应用

打开浏览器访问：**http://localhost:5173**

🎉 开始使用吧！

---

## 🛠️ 使用便捷脚本（推荐）

我们提供了启动脚本，更方便：

### macOS / Linux

```bash
# 启动项目
./start.sh

# 停止项目
./stop.sh
```

如果脚本无法执行，先添加权限：

```bash
chmod +x start.sh stop.sh
```

### 查看日志

```bash
# 后端日志
cd backend
npm run dev

# 前端日志
cd frontend  
npm run dev
```

---

## 📱 使用流程

1. **首页** → 点击"开始分析"
2. **上传页** → 上传 5-20 张朋友圈截图
3. **分析页** → 等待 AI 分析（约 20-30 秒）
4. **报告页** → 查看详细分析报告和追求建议

---

## ❓ 常见问题

### Q1: npm install 很慢怎么办？

使用国内镜像：

```bash
# 使用淘宝镜像
npm config set registry https://registry.npmmirror.com

# 或者使用 cnpm
npm install -g cnpm --registry=https://registry.npmmirror.com
cnpm install
```

### Q2: API Key 在哪里获取？

1. 访问 [火山引擎控制台](https://console.volcengine.com/ark)
2. 注册/登录账号
3. 进入"模型推理"页面
4. 创建 API Key
5. 确保有 `doubao-seed-1.6-thinking` 模型的访问权限

### Q3: 端口被占用怎么办？

**后端端口（3001）被占用**：

```bash
# 修改 backend/.env 中的 PORT
PORT=3002
```

**前端端口（5173）被占用**：

```bash
# Vite 会自动使用下一个可用端口
# 或手动修改 frontend/vite.config.js
```

### Q4: 分析一直失败？

检查：
1. ✅ API Key 是否正确配置
2. ✅ 网络是否正常
3. ✅ API 额度是否充足
4. ✅ 查看后端终端的错误日志

### Q5: 图片上传失败？

检查：
1. ✅ 图片格式（仅支持 JPG/PNG/WebP）
2. ✅ 图片大小（单张 < 10MB）
3. ✅ 图片数量（5-20 张）

---

## 🔧 开发模式

### 热重载

- **前端**：Vite 自动热重载
- **后端**：使用 nodemon（已配置）

修改代码后自动刷新，无需重启！

### 调试

**前端调试**：
- 浏览器 DevTools（F12）
- Vue DevTools 插件

**后端调试**：
- 查看终端日志
- 使用 `console.log()` 输出调试信息

---

## 📦 生产环境部署

参考 `DEPLOY.md` 文档。

---

## 🎨 技术栈

- **前端**：Vue 3 + Vite + Pinia + Vue Router
- **后端**：Node.js + Express + Multer
- **AI**：豆包大模型 (doubao-seed-1.6-thinking)
- **设计**：Apple Design System

---

## 💡 提示

- 建议使用 **Chrome** 或 **Safari** 浏览器
- 上传的截图越多越详细，分析结果越准确
- 截图内容要多样化（不要都是同一类型）

---

## 📞 获得帮助

- 📖 查看完整文档：`README.md`
- 🐛 提交问题：GitHub Issues
- 💬 交流讨论：项目讨论区

---

祝你使用愉快！如果觉得有用，别忘了给项目点个 ⭐

