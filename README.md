# 朋友圈恋爱助手 💕

一款基于 AI 大模型的 H5 应用，通过分析朋友圈截图，帮助用户更好地了解心仪对象，提供科学的追求建议。

![Version](https://img.shields.io/badge/version-1.0.0-blue)
![License](https://img.shields.io/badge/license-MIT-green)

## ✨ 功能特点

- 🖼️ **智能图片上传**：支持 5-20 张朋友圈截图上传，自动压缩优化
- 🤖 **AI 深度分析**：基于豆包大模型 `doubao-seed-1.6-thinking` 进行多维度分析
- 📊 **可视化报告**：美观的分析报告，包含评分、标签、建议等
- 💡 **实用建议**：提供破冰话题、约会建议、送礼指南等实用内容
- 🎨 **Apple 设计风格**：简洁优雅的界面，极致的用户体验
- 🔒 **隐私保护**：上传图片 24 小时后自动删除

## 📸 截图预览

### 首页
- 简洁的 Hero 区域
- 核心功能介绍
- 使用步骤说明

### 上传页面
- 拖拽/点击上传
- 实时预览
- 图片数量控制

### 分析页面
- 优雅的加载动画
- 实时进度显示
- 趣味提示文案

### 报告页面
- 综合评分可视化
- 性格标签展示
- 详细追求策略
- 行动计划时间线

## 🛠️ 技术栈

### 前端
- **框架**：Vue 3（Composition API）
- **构建工具**：Vite
- **状态管理**：Pinia
- **路由**：Vue Router 4
- **HTTP 客户端**：Axios
- **图片压缩**：Compressor.js

### 后端
- **运行时**：Node.js 18+
- **框架**：Express
- **文件上传**：Multer
- **AI 服务**：豆包大模型 API

## 📦 项目结构

```
demo/
├── frontend/                 # 前端项目
│   ├── src/
│   │   ├── assets/          # 静态资源
│   │   │   └── styles/      # 样式文件
│   │   ├── stores/          # Pinia 状态管理
│   │   ├── views/           # 页面组件
│   │   │   ├── Home.vue     # 首页
│   │   │   ├── Upload.vue   # 上传页
│   │   │   ├── Analyzing.vue # 分析页
│   │   │   └── Report.vue   # 报告页
│   │   ├── router/          # 路由配置
│   │   ├── App.vue          # 根组件
│   │   └── main.js          # 入口文件
│   ├── index.html
│   ├── package.json
│   └── vite.config.js
│
├── backend/                  # 后端项目
│   ├── routes/              # 路由
│   │   └── analyze.js       # 分析接口
│   ├── services/            # 服务层
│   │   └── doubao.js        # 豆包 AI 集成
│   ├── utils/               # 工具函数
│   │   └── imageUtils.js    # 图片处理
│   ├── uploads/             # 上传文件目录（自动创建）
│   ├── server.js            # 服务器入口
│   ├── package.json
│   ├── .env                 # 环境变量
│   └── .env.example         # 环境变量示例
│
├── PRD.md                    # 产品需求文档
└── README.md                 # 项目说明文档
```

## 🚀 快速开始

### 前置要求

- Node.js 18.0 或更高版本
- npm 或 yarn 包管理器
- 豆包大模型 API Key（[获取地址](https://console.volcengine.com/ark)）

### 1. 克隆项目

```bash
# 如果已经同步到 GitHub
git clone https://github.com/yourusername/moments-love-assistant.git
cd moments-love-assistant

# 如果是本地项目
cd demo
```

### 2. 配置后端

```bash
# 进入后端目录
cd backend

# 安装依赖
npm install

# 配置环境变量
# 编辑 .env 文件，填写你的 ARK_API_KEY
nano .env

# 或者复制示例文件
cp .env.example .env
# 然后编辑 .env 文件，填写 API Key
```

**重要**：在 `.env` 文件中配置你的豆包 API Key：

```env
ARK_API_KEY=your_actual_api_key_here
```

### 3. 配置前端

```bash
# 打开新终端，进入前端目录
cd frontend

# 安装依赖
npm install
```

### 4. 启动项目

**启动后端**（终端 1）：

```bash
cd backend
npm run dev
```

后端将在 `http://localhost:3001` 运行

**启动前端**（终端 2）：

```bash
cd frontend
npm run dev
```

前端将在 `http://localhost:3000` 运行

### 5. 访问应用

打开浏览器访问：`http://localhost:3000`

## 🔧 配置说明

### 豆包 API 配置

1. 访问 [火山引擎控制台](https://console.volcengine.com/ark)
2. 创建或获取 API Key
3. 确保 API Key 有调用 `doubao-seed-1.6-thinking` 模型的权限
4. 将 API Key 配置到 `backend/.env` 文件中

### 环境变量说明

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| `ARK_API_KEY` | 豆包 API 密钥 | 无（必填） |
| `ARK_BASE_URL` | API 基础地址 | `https://ark.cn-beijing.volces.com/api/v3` |
| `PORT` | 后端服务端口 | `3001` |
| `NODE_ENV` | 运行环境 | `development` |
| `UPLOAD_DIR` | 上传文件目录 | `./uploads` |

## 📝 API 文档

### POST /api/analyze

分析朋友圈截图

**请求**：
- Method: `POST`
- Content-Type: `multipart/form-data`
- Body: `images` (文件数组，5-20 张)

**响应**：

```json
{
  "scores": {
    "personality": "75%",
    "interest": "80%",
    "difficulty": "60%",
    "potential": "85%"
  },
  "tags": ["开朗", "爱运动", "文艺"],
  "characterAnalysis": "...",
  "interests": [...],
  "strategy": {
    "iceBreaker": "...",
    "topics": [...],
    "dates": [...],
    "gifts": "...",
    "warnings": [...]
  },
  "actionPlan": [...]
}
```

### GET /api/health

健康检查

**响应**：

```json
{
  "status": "healthy",
  "timestamp": "2025-12-10T12:00:00.000Z"
}
```

## 🎨 UI 设计规范

### 设计风格

本项目采用 Apple 设计风格，核心特点：

- **简洁**：去除不必要的装饰，专注内容
- **优雅**：圆角、阴影、渐变的精致运用
- **流畅**：平滑的过渡动画，自然的交互反馈
- **舒适**：合理的间距、字号、配色

### 色彩系统

```css
--primary-color: #FF2D55      /* 主色 - 温暖粉红 */
--primary-light: #FF6B8A      /* 主色 - 浅色 */
--secondary-color: #5E5CE6    /* 辅助色 - 紫色 */
--accent-color: #BF5AF2       /* 强调色 */

--text-primary: #1d1d1f       /* 主文字 */
--text-secondary: #6e6e73     /* 次要文字 */
--text-tertiary: #86868b      /* 三级文字 */
```

### 排版规范

- **大标题**：48px / 36px（移动端）
- **二级标题**：36px / 28px（移动端）
- **三级标题**：28px / 20px（移动端）
- **正文**：16px
- **小字**：14px
- **行高**：1.5 - 1.8

## 🔒 隐私与安全

- ✅ 所有上传的图片仅用于 AI 分析
- ✅ 图片存储在服务器本地，24 小时后自动删除
- ✅ 不收集用户个人信息
- ✅ 不保存分析结果（仅在前端临时存储）
- ✅ 使用 HTTPS 加密传输（生产环境）

## 📱 浏览器支持

- ✅ iOS Safari 12+
- ✅ Android Chrome 70+
- ✅ 微信内置浏览器
- ✅ Chrome / Edge / Firefox（最新版本）

## 🚢 部署指南

### 前端部署（Vercel / Netlify）

1. 将前端代码推送到 GitHub
2. 在 Vercel/Netlify 中导入项目
3. 构建命令：`npm run build`
4. 输出目录：`dist`
5. 环境变量：配置后端 API 地址（如需要）

### 后端部署（云服务器）

1. 准备一台云服务器（阿里云/腾讯云）
2. 安装 Node.js 18+
3. 克隆代码到服务器
4. 配置 `.env` 文件
5. 安装依赖：`npm install --production`
6. 使用 PM2 启动：
   ```bash
   npm install -g pm2
   pm2 start server.js --name moments-backend
   pm2 save
   pm2 startup
   ```
7. 配置 Nginx 反向代理
8. 配置 HTTPS 证书（Let's Encrypt）

### Docker 部署

```bash
# 构建前端
cd frontend
docker build -t moments-frontend .

# 构建后端
cd backend
docker build -t moments-backend .

# 使用 docker-compose 启动
docker-compose up -d
```

## 🐛 常见问题

### Q1: API Key 配置后仍然提示未配置？

**A**: 检查以下几点：
1. `.env` 文件是否在 `backend` 目录下
2. `ARK_API_KEY` 是否正确填写（无空格）
3. 重启后端服务

### Q2: 上传图片后一直卡在分析页面？

**A**: 可能原因：
1. API Key 无效或额度不足
2. 网络问题，无法连接豆包 API
3. 查看浏览器控制台和后端日志排查

### Q3: 分析结果不准确？

**A**: 建议：
1. 确保上传的截图清晰、内容丰富
2. 至少上传 10 张以上截图
3. 截图内容要多样化（不要都是同一类型）

### Q4: 移动端显示异常？

**A**: 确保：
1. 使用现代浏览器（Safari/Chrome）
2. 浏览器视口缩放为 100%
3. 清除浏览器缓存后重试

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

1. Fork 本项目
2. 创建特性分支：`git checkout -b feature/AmazingFeature`
3. 提交更改：`git commit -m 'Add some AmazingFeature'`
4. 推送到分支：`git push origin feature/AmazingFeature`
5. 提交 Pull Request

## 📄 开源协议

本项目采用 MIT 协议开源。

## 🙏 致谢

- [Vue.js](https://vuejs.org/) - 渐进式 JavaScript 框架
- [Express](https://expressjs.com/) - Node.js Web 框架
- [豆包大模型](https://www.volcengine.com/product/doubao) - AI 能力支持
- Apple Design - 设计灵感来源

## 📧 联系方式

如有问题或建议，欢迎联系：

- 项目地址：[GitHub](https://github.com/yourusername/moments-love-assistant)
- Issue：[提交 Issue](https://github.com/yourusername/moments-love-assistant/issues)

---

**免责声明**：本项目仅供学习交流使用，请遵守相关法律法规，尊重他人隐私。使用本工具时，请确保已获得相关人员的同意。

Made with ❤️ by Your Team

